-- Kissan Rehnuma — initial databases
-- kissan_auth is created by POSTGRES_DB (see docker-compose.yml);
-- this script creates the two remaining service databases.
CREATE DATABASE kissan_crop_disease;
CREATE DATABASE kissan_animal_disease;
